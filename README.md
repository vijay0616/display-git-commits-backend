# Getting Started with Git commits display application - backend

This is the backend repository

## Getting Started

In the project directory, you can run:

### `npm install`

Then run the following command:

### `node app.js`
